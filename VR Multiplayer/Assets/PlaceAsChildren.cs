using UnityEngine;


public class PlaceAsChildren : MonoBehaviour
{
    private GameObject localOrigin;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
   

    // Update is called once per frame
    void Update()
    {
        
    }
}
